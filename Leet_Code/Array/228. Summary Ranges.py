from typing import List


class Solution:
    def summaryRanges(self, nums: List[int]) -> List[str]:
        res = []
        path = []
        n = len(nums)
        for i in range(n):
            st = 
            if i < n-1 and 