class Solution:
    def characterReplacement(self, s: str, k: int) -> int:

        res = 0
        L = 0
        remain = k
        checks = {}
        for R in range(len(s)):
            
            if s[R] not in check:


Solution().characterReplacement("AABBBC", 1)
            